export type SoundOptions = {
  force?: boolean;
};

export type NotificationSoundSettingsResponse = {
  notificationSoundEnabled: boolean;
};

export type MessageSoundSettingsResponse = {
  messageSoundEnabled: boolean;
};
